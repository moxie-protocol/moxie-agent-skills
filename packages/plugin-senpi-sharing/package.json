{
    "name": "@senpi-ai/plugin-senpi-sharing",
    "version": "0.1.0",
    "description": "A plugin for creating and sharing summarized content across social platforms",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "dependencies": {
        "@senpi-ai/core": "workspace:*",
        "@senpi-ai/senpi-agent-lib": "workspace:*"
    },
    "devDependencies": {
        "tsup": "^8.3.5"
    },
    "scripts": {
        "build": "tsup --format esm --dts",
        "dev": "tsup --format esm --dts --watch"
    }
}
