{
  "name": "@moxie-protocol/plugin-senpi-sharing",
  "version": "0.1.0",
  "description": "A plugin for creating and sharing summarized content across social platforms",
  "main": "dist/index.js",
  "type": "module",

  "types": "dist/index.d.ts",
  "dependencies": {
    "@moxie-protocol/core": "workspace:*",
    "@moxie-protocol/moxie-agent-lib": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5"
},
  "scripts": {
    "build": "tsup --format esm --dts",
    "dev": "tsup --format esm --dts --watch"
}
} 